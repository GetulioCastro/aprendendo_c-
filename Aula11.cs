using System;

class Aula11 {
  static void Main () {
      // int n1=10;
      // float n2=n1; // conversão implícita

      // float n1=10.5f;
      // int n2=(int)n1; // conversão do tipo CAST

      int vInt=10;
      short vShort=(short)vInt;
      Console.WriteLine (vShort);
  }
}